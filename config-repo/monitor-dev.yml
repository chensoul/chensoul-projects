server:
  port: 5100

spring.security:
  user:
    name: admin
    password: 123456

spring.boot.admin:
  discovery:
    ignored-services:
      - adminserver
  ui:
    title: ${spring.application.name}
