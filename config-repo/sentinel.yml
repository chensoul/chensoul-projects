server:
  port: 5300

auth:
  username: user
  password: 123456
  filter:
    exclude-urls: /,/authorization/login,/authorization/logout,/registry/machine,/version,/actuator/**,/details
    exclude-url-suffixes: htm,html,js,css,map,ico,ttf,woff,png

sentinel:
  dashboard:
    # 配置 sentinel 控制台版本号
    version: 1.8.0
